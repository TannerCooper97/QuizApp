<p style="color: green"><%= notice %></p>

<%= render @quiz %>

<div class="d-flex justify-content-center mt-2">
  <%= link_to "Edit", edit_quiz_path(@quiz), class: "btn btn-warning mx-1" %>
  <%= link_to "Back to Quizzes", quizzes_path, class: "btn btn-primary mx-3" %>
  <%= button_to "Delete", @quiz, method: :delete, class: "btn btn-danger" %>
</div>
